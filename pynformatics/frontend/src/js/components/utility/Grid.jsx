import React from 'react';
import { Row, Col } from 'antd';


const rowStyle = {
  display: 'flex',
  flexFlow: 'row wrap',
};

const colStyle = {
  marginBottom: '16px',
};

const gutter = 16;

export {
  gutter,
  colStyle,
  rowStyle,
  Col,
  Row,
}
